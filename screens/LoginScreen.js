import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const LoginScreen = () => {
    return (
        <View>
            <Text>this is the Login Screen</Text>
        </View>
    )
}

export default LoginScreen;

//Move this to a different folder later
const styles = StyleSheet.create({})